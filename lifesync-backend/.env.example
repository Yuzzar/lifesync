# LifeSync Backend - Environment Variables (optional)

# Server Configuration
PORT=8080
ENVIRONMENT=development

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=1234
DB_NAME=lifesync
DB_SSLMODE=disable

# CORS Configuration (comma-separated)
CORS_ORIGINS=*
